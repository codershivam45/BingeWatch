export const metadata = {
    title: "Shows - Airing Today",
    description: "Catch all the TV shows airing today. See what's on tonight and never miss a new episode!",
};

const AiringTodayLayout = ({ children }) => {
    return <>{children}</>;
};

export default AiringTodayLayout;
  
export const metadata = {
    title: "Shows - On TV",
    description: "Find out what TV shows are currently airing live. Stay in the loop with what's broadcasting now on television!",
};

const OnTVShowsLayout = ({ children }) => {
    return <>{children}</>;
};

export default OnTVShowsLayout;
  
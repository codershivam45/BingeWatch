export const metadata = {
    title: "Shows - Popular",
    description: "Explore the most popular TV shows everyone is watching right now. Stay up to date with the latest fan favorites!",
};

const PopularShowsLayout = ({ children }) => {
    return <>{children}</>;
};

export default PopularShowsLayout;
  
export const metadata = {
    title: "Shows - Top Rated",
    description: "Browse the highest-rated TV shows of all time. Discover critically acclaimed series worth binge-watching!",
};

const TopRatedShowsLayout = ({ children }) => {
    return <>{children}</>;
};

export default TopRatedShowsLayout;
  
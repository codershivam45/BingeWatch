export const metadata = {
    title: "Movies - Now Playing",
    description: "Discover all the latest movies currently playing in theaters. Stay updated with what's trending on the big screen!",
};
  
const PlayingMovieLayout = ({ children }) => {
    return <>{children}</>;
};

export default PlayingMovieLayout;
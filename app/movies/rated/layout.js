export const metadata = {
    title: "Movies - Top Rated",
    description: "Browse the highest-rated movies of all time. Discover critically acclaimed cinema worth watching!",
};

const TopRatedMovieLayout = ({ children }) => {
    return <>{children}</>;
};

export default TopRatedMovieLayout;
  
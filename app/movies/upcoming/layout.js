export const metadata = {
    title: "Movies - Upcoming",
    description: "Get a sneak peek at upcoming movie releases. Never miss out on what's hitting theaters next!",
};

const UpcomingMovieLayout = ({ children }) => {
    return <>{children}</>;
};

export default UpcomingMovieLayout;
